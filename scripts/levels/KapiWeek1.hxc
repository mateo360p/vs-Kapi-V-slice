package dipazio.level;

import dipazio.level.DpzLevel;
import funkin.ui.story.Level;
import funkin.save.Save;
import flixel.FlxG;
import funkin.ui.story.StoryMenuState;

class KapiWeekOneLevel extends DpzLevel
{
  function new() {
    super('KapiWeek1');
    freeplaySongs = ["fuzzy-distortion"];
    freeplayUnlocked = true;
  }
}
